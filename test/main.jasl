/*import std;

let cPtr: *fn type -> type = fn |T| #(@c) *T;
let toSlice = fn (T: type, ptr: *T, len: u32) -> []T {
    return ptr[0..len];
};

let main = fn () -> Main {
    let cStylePtr: cPtr(u32) = undefined;
    let cSlice = toSlice(u32, cStylePtr, 32);

    return Main(@Ok);
};

let Main = std::Result(void, std::ErrorCode);
*/

let main = @Test;
