pub let TypeInfo = union(enum) {
    Struct: Struct,
    Union: Union,
    Enum: Enum,

    Integer: Integer,
    Bool: bool, // mutability bool
    Float: bool, // mutability bool
    Void: void,

    Pointer: Pointer,
    Function: Function,
    Noreturn: void,
    Any: void,
    Type: void,
};

pub let FieldInfo = struct {
    pub public: bool,
    pub name: []u8,
    pub valueType: type,
};

pub let Struct = struct {
    pub mutable: bool,
    pub name: []u8,
    pub fields: []FieldInfo,
    pub definitions: []FieldInfo,
};

pub let Union = Struct;

pub let Enum = struct {
    pub mutable: bool,
    pub name: []u8,
    pub fields: [][]u8,
    pub definitions: []FieldInfo,
};

pub let Pointer = struct {
    pub mutable: bool,
    pub child: type,
    pub pointerType: enum {
        Slice,
        Single,
    },
};

pub let Function = struct {
    pub mutable: bool,
    pub name: []u8,
    pub argTypes: []type,
    pub returnTypes: []type,
};

pub let Integer = struct {
    pub mutable: bool,
    pub size: u8,
    pub signed: bool,
};
